import { Navbar } from '../../components/Navbar';
import { Hero } from '../../components/home/Hero';

export const Home = () => {
    return (
        <main>
            <Navbar />
            <Hero />
        </main>
    )
}