import { Options } from "../../components/signup/Options"
import { Navbar } from "../../components/Navbar"

export const SignUp = () => {
    return (
        <main>
            <Navbar />
            <Options />
        </main>
    )
}