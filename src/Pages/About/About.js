import { AboutUs } from '../../components/AboutUs';
import { Navbar } from '../../components/Navbar';

export const About = () => {
    return (
        <main>
            <Navbar />
            <AboutUs />
        </main>
    )
}