import { Navbar } from '../../components/Navbar';
import { Contact } from '../../components/Contact';

export const ContactUs = () => {
    return (
        <main>
            <Navbar />
            <Contact />
        </main>
    )
}